# Open Questions

## Open owner decisions

1. **Final title/repository name:** `Wayroot Exploration` / `wayroot-exploration` is a working title derived from provisional lore. Rename later before branding, bundle IDs, or public asset naming become permanent.
2. **Pinned editor:** install/confirm Unity 6000.0.78f1 through Hub and maintain it throughout Phase 0/1. Do not migrate via installed 6000.5.4f1.
3. **Runtime UI:** Phase 1 needs a narrow, documented uGUI vs UI Toolkit touch-control spike. Default recommendation: Unity UI/uGUI for familiar touch ergonomics.
4. **Placeholder character:** primitives only, or an approved/licensed placeholder asset? No unvalidated asset may enter production tracking.
5. **iPhone worker:** identify compatible Mac/Xcode machine and signing owner before Phase 1’s mobile checkpoint.

## Resolved in Phase 0

Unity 6.0 LTS / 6000.0.78f1; URP; Input System; versioned plain save records plus seed/delta world persistence; ScriptableObjects for static authored definitions and plain C# types for mutable state.
